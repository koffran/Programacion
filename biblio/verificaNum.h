/** \brief Verifica que el divisor sea un numero diferente a cero
 *
 * \param Recibe un divisor como parametro
 * \return En caso de que el divisor sea cero regresa un numero diferente
 *
 */
float validaDivision(float numero);

/** \brief Verifica que el numero ingresado sea entero
 *
 * \param recibe un numero flotante
 * \return regresa un numero entero ingresado por el usuario
 *
 */
int validaFactorial(float);
